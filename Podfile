# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RSCustomAlertView' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  pod 'RSCustomAlertSheetViewLib', '~> 0.5.0'

  # Pods for RSCustomAlertView

  target 'RSCustomAlertViewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RSCustomAlertViewUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
